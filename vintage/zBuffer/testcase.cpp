#include <bits/stdc++.h>
using namespace std;

#define AKY AayushKYadav ^_^

#define IOS ios::sync_with_stdio(false); cin.tie(nullptr);
#define int long long
#define newline <<'\n'
#define all(x) (x).begin(),(x).end()
const int INF = LLONG_MAX >> 1;

void solve() {
    int n = 100; // Change 'n' to adjust the number of rows in the pattern
    cout << n newline;

    for (int i = 0; i < n; ++i) {
        cout << 0 << " " << i+1 newline;
    }
}

int32_t main() {
    IOS;

    int t = 1;
    while (t--) {
        solve();
    }

    return 0;
}
